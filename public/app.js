const state={tenantId:localStorage.getItem('tenantId')||'sentire-customs-broker',data:null,page:'dashboard',draft:{},calc:null};
const $=s=>document.querySelector(s);const money=n=>`USD ${Number(n||0).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2})}`;const pct=n=>`${Number(n||0)}%`;
async function api(url,opt={}){opt.headers={...(opt.headers||{}),'x-tenant-id':state.tenantId};if(opt.body&&!(opt.body instanceof FormData)){opt.headers['content-type']='application/json';opt.body=JSON.stringify(opt.body)}const r=await fetch(url,opt);if(!r.ok){let e;try{e=await r.json()}catch{e={error:await r.text()}}throw new Error(e.error||'Error');}return r.headers.get('content-type')?.includes('application/json')?r.json():r;}
async function boot(){try{state.data=await api('/api/bootstrap');$('#tenantSelect').value=state.tenantId;applyTenant();renderNav();route('dashboard');}catch(e){document.querySelector('#content').innerHTML=`<div class="content"><div class="notice">Error iniciando: ${e.message}</div></div>`}}
function applyTenant(){const t=state.data.tenant;$('#tenantLogo').src=t.logo;$('#tenantNameSide').textContent=t.name;$('#tenantCardName').textContent=t.name;}
function renderNav(){const isLog=state.data.tenant.module==='logistics';const nav=[['dashboard','▦ Dashboard'],['products','◫ Productos'],['quotes','▤ Cotizaciones'],...(isLog?[['logistics','⚓ Cotizaciones logísticas'],['profiles','⚙ Perfiles y permisos']]:[['catalog','▥ Catálogos'],['profiles','⚙ Perfiles y permisos']]),['clients','♙ Clientes']];$('#nav').innerHTML=nav.map(([id,label])=>`<button class="nav-item ${state.page===id?'active':''}" onclick="route('${id}')"><span>${label}</span></button>`).join('')}
function route(page){state.page=page;renderNav();const fn={dashboard,products,quotes,logistics,profiles,catalog,clients}[page]||dashboard;fn();}
function shell(title,sub,actions=''){$('#pageTitle').textContent=title;$('#content').innerHTML=`<div class="content"><div class="page-head"><div><h1>${title}</h1><p>${sub||''}</p></div><div>${actions}</div></div><div id="pageBody"></div></div>`}
function dashboard(){shell(state.data.tenant.module==='logistics'?'Dashboard Logística':'Dashboard','Resumen operativo de MRAPI Quotes.');const d=state.data;const quotes=d.quotes||[];$('#pageBody').innerHTML=`<div class="grid kpis"><div class="kpi"><small>Productos cargados</small><strong>${d.products.length}</strong></div><div class="kpi"><small>Cotizaciones</small><strong>${quotes.length}</strong></div><div class="kpi"><small>Perfiles logísticos</small><strong>${d.logisticsProfiles.length}</strong></div><div class="kpi"><small>Perfiles impositivos</small><strong>${d.taxProfiles.length}</strong></div></div><br><div class="grid split"><div class="card"><h3>Cotizaciones recientes</h3>${quotesTable(quotes.slice(0,10))}</div><div class="card"><h3>Acciones rápidas</h3><div class="actions"><button class="btn primary" onclick="newQuote('${state.data.tenant.module==='logistics'?'logistics':'product'}')">+ Nueva cotización</button><button class="btn" onclick="route('products')">Productos</button><button class="btn" onclick="route('profiles')">Perfiles</button></div><div class="notice">Los cambios de perfiles se aplican a nuevas cotizaciones. Las emitidas conservan snapshot.</div></div></div>`}
function quotesTable(qs){return `<div class="table-wrap"><table><thead><tr><th>Proforma</th><th>Cliente</th><th>Tipo</th><th>FOB</th><th>Total</th><th>Estado</th><th></th></tr></thead><tbody>${qs.length?qs.map(q=>`<tr><td>${q.quoteNo||q.id}</td><td>${q.clientName||'-'}</td><td>${q.mode||q.logisticsProfileSnapshot?.name||'-'}</td><td>${money(q.calculation?.fob)}</td><td>${money(q.calculation?.totalToPay)}</td><td><span class="status">${q.status||'draft'}</span></td><td>${q.id?`<button class="ghost" onclick="window.open('/api/quotes/${q.id}/pdf?tenantId=${state.tenantId}')">PDF</button>`:''}</td></tr>`).join(''):`<tr><td colspan="7">Todavía no hay cotizaciones.</td></tr>`}</tbody></table></div>`}
function products(){shell('Base de productos','Productos para cotizaciones y catálogos.',`<button class="btn primary" onclick="productModal()">+ Nuevo producto</button> <button class="btn" onclick="importModal()">Carga masiva</button>`);const ps=state.data.products||[];$('#pageBody').innerHTML=`<div class="card"><div class="toolbar"><input id="psearch" placeholder="Buscar SKU o producto..." oninput="filterProducts()"><span>${ps.length} productos</span></div><div id="ptable">${productTable(ps)}</div></div>`}
function productTable(ps){return `<div class="table-wrap"><table><thead><tr><th>Foto</th><th>SKU</th><th>Producto</th><th>Categoría</th><th>FOB</th><th>CBM</th><th>Perfil impositivo</th><th></th></tr></thead><tbody>${ps.map(p=>`<tr><td><img class="thumb" src="${p.imageUrl||'/assets/shenzhen-logo.png'}"></td><td>${p.sku||p.id}</td><td>${p.name}</td><td>${p.category||'-'}</td><td>${money(p.fob)}</td><td>${Number(p.cbm||0).toFixed(3)}</td><td>${p.taxProfileId||'general'}</td><td><button class="ghost" onclick="productModal('${p.id}')">Editar</button></td></tr>`).join('')||`<tr><td colspan="8">Sin productos todavía.</td></tr>`}</tbody></table></div>`}
function filterProducts(){const q=$('#psearch').value.toLowerCase();$('#ptable').innerHTML=productTable(state.data.products.filter(p=>`${p.sku} ${p.name} ${p.category}`.toLowerCase().includes(q)))}
function productModal(pid){const p=state.data.products.find(x=>x.id===pid)||{};openModal(pid?'Editar producto':'Nuevo producto',`<div class="form-grid"><div class="field"><label>SKU</label><input id="pSku" value="${p.sku||''}"></div><div class="field span2"><label>Producto</label><input id="pName" value="${p.name||''}"></div><div class="field"><label>Categoría</label><input id="pCat" value="${p.category||''}"></div><div class="field"><label>FOB USD</label><input type="number" step=".01" id="pFob" value="${p.fob||0}"></div><div class="field"><label>CBM</label><input type="number" step=".001" id="pCbm" value="${p.cbm||0}"></div><div class="field"><label>KG</label><input type="number" step=".01" id="pKg" value="${p.kg||0}"></div><div class="field"><label>MOQ</label><input type="number" id="pMoq" value="${p.moq||0}"></div><div class="field span2"><label>Perfil impositivo</label><select id="pTax">${state.data.taxProfiles.map(x=>`<option value="${x.id}" ${p.taxProfileId===x.id?'selected':''}>${x.name}</option>`).join('')}</select></div><div class="field span2"><label>Perfil logístico</label><select id="pLog"><option value="">Sin perfil</option>${state.data.logisticsProfiles.map(x=>`<option value="${x.id}" ${p.logisticsProfileId===x.id?'selected':''}>${x.name}</option>`).join('')}</select></div><div class="field span4"><label>URL imagen</label><input id="pImage" value="${p.imageUrl||''}"></div></div><br><button class="btn primary" onclick="saveProduct('${pid||''}')">Guardar producto</button>`)}
async function saveProduct(pid){const body={id:pid||$('#pSku').value.trim(),sku:$('#pSku').value.trim(),name:$('#pName').value.trim(),category:$('#pCat').value,fob:+$('#pFob').value,cbm:+$('#pCbm').value,kg:+$('#pKg').value,moq:+$('#pMoq').value,taxProfileId:$('#pTax').value,logisticsProfileId:$('#pLog').value,imageUrl:$('#pImage').value,active:true};await api('/api/products',{method:'POST',body});closeModal();await refresh();route('products')}
function importModal(){openModal('Carga masiva de productos',`<div class="upload"><h3>Excel / CSV</h3><p>Columnas recomendadas: SKU, Producto, Categoria, FOB, CBM, KG, MOQ, PerfilImpositivo, PerfilLogistico, Imagen</p><input type="file" id="importFile" accept=".xlsx,.xls,.csv"></div><br><button class="btn primary" onclick="doImport()">Validar y cargar</button><div id="importResult"></div>`)}
async function doImport(){const f=$('#importFile').files[0];if(!f)return alert('Elegí un archivo');const fd=new FormData();fd.append('file',f);fd.append('tenantId',state.tenantId);const r=await api('/api/products/import',{method:'POST',body:fd});$('#importResult').innerHTML=`<p><b>${r.imported}</b> importados de ${r.processed}. Errores: ${r.errors.length}</p>${r.errors.length?`<pre>${JSON.stringify(r.errors.slice(0,20),null,2)}</pre>`:''}`;await refresh();}
function profiles(){shell('Perfiles y permisos','Lógica logística e impositiva separadas y editables.',`<button class="btn primary" onclick="taxModal()">+ Perfil impositivo</button> <button class="btn" onclick="logModal()">+ Perfil logístico</button>`);$('#pageBody').innerHTML=`<div class="grid split"><div><div class="card"><h3>Perfiles logísticos</h3>${logProfilesTable()}</div><br><div class="card"><h3>Perfiles impositivos</h3>${taxProfilesTable()}</div></div><div class="card"><h3>Acceso por usuario</h3><p style="color:var(--muted)">MVP listo para guardar usuarios y permisos por perfil. La UI de asignación queda preparada para la siguiente iteración.</p><div class="notice"><b>Versionado y snapshot</b><br>Una cotización guarda copia de los valores usados al momento de emitirla.</div></div></div>`}
function taxProfilesTable(){return `<div class="table-wrap"><table><thead><tr><th>Nombre</th><th>Derecho</th><th>IVA</th><th>IVA ADD</th><th>Ganancia</th><th>IIBB</th><th>Tasa</th><th>Honorarios</th><th></th></tr></thead><tbody>${state.data.taxProfiles.map(p=>`<tr><td>${p.name}</td><td>${pct(p.duty)}</td><td>${pct(p.vat)}</td><td>${pct(p.vatAdditional)}</td><td>${pct(p.earnings)}</td><td>${pct(p.iibb)}</td><td>${pct(p.statisticalFee)}</td><td>${p.honorariaApplies?'Sí':'No'}</td><td><button class="ghost" onclick="taxModal('${p.id}')">Editar</button></td></tr>`).join('')}</tbody></table></div>`}
function logProfilesTable(){return `<div class="table-wrap"><table><thead><tr><th>Nombre</th><th>Tipo</th><th>Ruta</th><th>Unidad</th><th>Costo referencia</th><th></th></tr></thead><tbody>${state.data.logisticsProfiles.map(p=>`<tr><td>${p.name}</td><td>${p.type}</td><td>${p.route}</td><td>${p.unit}</td><td>${money((p.lines||[]).reduce((s,l)=>s+(l.basis==='cbm'?l.amount:0),0))}/CBM</td><td><button class="ghost" onclick="logModal('${p.id}')">Editar</button></td></tr>`).join('')}</tbody></table></div>`}
function taxModal(pid){const p=state.data.taxProfiles.find(x=>x.id===pid)||{name:'General',duty:18,vat:21,vatAdditional:20,earnings:6,iibb:3,statisticalFee:3,honorariaApplies:true,honorariaType:'fixed',honorariaValue:200};openModal(pid?'Editar perfil impositivo':'Nuevo perfil impositivo',`<div class="form-grid"><div class="field span2"><label>Nombre</label><input id="tName" value="${p.name}"></div>${[['Derecho','duty'],['IVA','vat'],['IVA adicional','vatAdditional'],['Ganancia','earnings'],['IIBB','iibb'],['Tasa estadística','statisticalFee']].map(([l,k])=>`<div class="field"><label>${l} %</label><input type="number" step=".01" id="t_${k}" value="${p[k]||0}"></div>`).join('')}<div class="field"><label>Honorarios aplica</label><select id="tHon"><option value="1" ${p.honorariaApplies?'selected':''}>Sí</option><option value="0" ${!p.honorariaApplies?'selected':''}>No</option></select></div><div class="field"><label>Tipo honorarios</label><select id="tHonType"><option value="fixed" ${p.honorariaType==='fixed'?'selected':''}>Monto fijo</option><option value="percent" ${p.honorariaType==='percent'?'selected':''}>Porcentaje</option></select></div><div class="field"><label>Valor honorarios</label><input type="number" step=".01" id="tHonVal" value="${p.honorariaValue||0}"></div></div><br><button class="btn primary" onclick="saveTax('${pid||''}')">Guardar perfil</button>`)}
async function saveTax(pid){const body={id:pid||undefined,name:$('#tName').value,duty:+$('#t_duty').value,vat:+$('#t_vat').value,vatAdditional:+$('#t_vatAdditional').value,earnings:+$('#t_earnings').value,iibb:+$('#t_iibb').value,statisticalFee:+$('#t_statisticalFee').value,honorariaApplies:$('#tHon').value==='1',honorariaType:$('#tHonType').value,honorariaValue:+$('#tHonVal').value,active:true};await api('/api/taxProfiles',{method:'POST',body});closeModal();await refresh();route('profiles')}
function logModal(pid){const p=state.data.logisticsProfiles.find(x=>x.id===pid)||{name:'Nuevo perfil',type:'LCL',route:'China → Argentina',unit:'CBM',lines:[{code:'freight',name:'Flete',basis:'cbm',amount:300},{code:'clearance',name:'Despacho',basis:'cbm',amount:90},{code:'terminal',name:'Terminal',basis:'cbm',amount:70},{code:'delivery',name:'Entrega final',basis:'cbm',amount:60}]};openModal(pid?'Editar perfil logístico':'Nuevo perfil logístico',`<div class="form-grid"><div class="field span2"><label>Nombre</label><input id="lName" value="${p.name}"></div><div class="field"><label>Tipo</label><input id="lType" value="${p.type}"></div><div class="field"><label>Unidad</label><select id="lUnit"><option ${p.unit==='CBM'?'selected':''}>CBM</option><option ${p.unit==='KG'?'selected':''}>KG</option><option ${p.unit==='container'?'selected':''}>container</option></select></div><div class="field span4"><label>Ruta</label><input id="lRoute" value="${p.route||''}"></div></div><br><h4>Componentes del costo</h4><div id="lines">${(p.lines||[]).map((l,i)=>lineHtml(l,i)).join('')}</div><button class="ghost" onclick="addLine()">+ Agregar concepto</button><br><br><button class="btn primary" onclick="saveLog('${pid||''}')">Guardar perfil</button>`)}
function lineHtml(l={},i=Date.now()){return `<div class="line-row" data-line><input placeholder="Concepto" value="${l.name||''}"><select><option value="fixed" ${l.basis==='fixed'?'selected':''}>Fijo</option><option value="cbm" ${l.basis==='cbm'?'selected':''}>Por CBM</option><option value="kg" ${l.basis==='kg'?'selected':''}>Por KG</option><option value="percent_fob" ${l.basis==='percent_fob'?'selected':''}>% FOB</option></select><input type="number" step=".01" value="${l.amount||0}"><button class="ghost" onclick="this.parentElement.remove()">✕</button></div>`}
function addLine(){$('#lines').insertAdjacentHTML('beforeend',lineHtml())}
async function saveLog(pid){const lines=[...document.querySelectorAll('[data-line]')].map((r,i)=>({code:i===0?'freight':`line_${i}`,name:r.children[0].value,basis:r.children[1].value,amount:+r.children[2].value}));const body={id:pid||undefined,name:$('#lName').value,type:$('#lType').value,unit:$('#lUnit').value,route:$('#lRoute').value,lines,active:true};await api('/api/logisticsProfiles',{method:'POST',body});closeModal();await refresh();route('profiles')}
function quotes(){shell('Cotizaciones','Historial de cotizaciones guardadas.',`<button class="btn primary" onclick="newQuote('product')">+ Nueva cotización</button>`);$('#pageBody').innerHTML=`<div class="card">${quotesTable(state.data.quotes)}</div>`}
function logistics(){shell('Cotizaciones logísticas','Cotizá LCL, FCL, aéreo, courier y fiscal con perfiles editables.',`<button class="btn primary" onclick="newQuote('logistics')">+ Nueva cotización logística</button>`);$('#pageBody').innerHTML=`<div class="card">${quotesTable(state.data.quotes)}</div>`}
function newQuote(mode='logistics'){state.draft={mode,taxProfileId:state.data.taxProfiles.find(x=>x.isDefault)?.id||state.data.taxProfiles[0]?.id,logisticsProfileId:state.data.logisticsProfiles[0]?.id,fob:0,cbm:1,kg:0,insurancePct:2,clientName:'',description:'',status:'draft'};renderQuoteBuilder()}
function renderQuoteBuilder(){shell(state.draft.mode==='logistics'?'Nueva cotización logística':'Nueva cotización de productos','Valores editables en tiempo real.',`<button class="btn" onclick="saveQuote(false)">Guardar borrador</button> <button class="btn primary" onclick="saveQuote(true)">Guardar y emitir</button>`);const d=state.draft;$('#pageBody').innerHTML=`<div class="grid split"><div><div class="card"><h3>Datos base</h3><div class="form-grid"><div class="field span2"><label>Cliente</label><input id="qClient" value="${d.clientName}" oninput="readQuote()"></div><div class="field span2"><label>Descripción</label><input id="qDesc" value="${d.description}" oninput="readQuote()"></div><div class="field"><label>FOB USD</label><input id="qFob" type="number" step=".01" value="${d.fob}" oninput="readQuote()"></div><div class="field"><label>CBM</label><input id="qCbm" type="number" step=".001" value="${d.cbm}" oninput="readQuote()"></div><div class="field"><label>KG</label><input id="qKg" type="number" step=".01" value="${d.kg}" oninput="readQuote()"></div><div class="field"><label>Seguro %</label><input id="qIns" type="number" step=".01" value="${d.insurancePct}" oninput="readQuote()"></div><div class="field span2"><label>Perfil impositivo</label><select id="qTax" onchange="readQuote()">${state.data.taxProfiles.map(x=>`<option value="${x.id}" ${d.taxProfileId===x.id?'selected':''}>${x.name}</option>`).join('')}</select></div><div class="field span2"><label>Perfil logístico</label><select id="qLog" onchange="readQuote()">${state.data.logisticsProfiles.map(x=>`<option value="${x.id}" ${d.logisticsProfileId===x.id?'selected':''}>${x.name}</option>`).join('')}</select></div></div></div><br><div class="card"><h3>Perfil impositivo</h3><div id="taxPreview"></div></div><br><div class="card"><h3>Gastos logísticos editables</h3><div id="logPreview"></div><p style="color:var(--muted)">Para cambiar tarifas base globales, editá el perfil. Acá se calcula la cotización actual.</p></div></div><div><div class="card"><h3>Resumen de la cotización</h3><div id="quoteSummary"></div></div></div></div>`;readQuote();}
function readQuote(){state.draft.clientName=$('#qClient')?.value||'';state.draft.description=$('#qDesc')?.value||'';state.draft.fob=+($('#qFob')?.value||0);state.draft.cbm=+($('#qCbm')?.value||0);state.draft.kg=+($('#qKg')?.value||0);state.draft.insurancePct=+($('#qIns')?.value||0);state.draft.taxProfileId=$('#qTax')?.value||state.draft.taxProfileId;state.draft.logisticsProfileId=$('#qLog')?.value||state.draft.logisticsProfileId;const t=state.data.taxProfiles.find(x=>x.id===state.draft.taxProfileId)||{};const l=state.data.logisticsProfiles.find(x=>x.id===state.draft.logisticsProfileId)||{};$('#taxPreview').innerHTML=`<div class="profile-row"><b>Derecho</b><span>${pct(t.duty)}</span><b>IVA</b><span>${pct(t.vat)}</span></div><div class="profile-row"><b>IVA adicional</b><span>${pct(t.vatAdditional)}</span><b>Ganancia</b><span>${pct(t.earnings)}</span></div><div class="profile-row"><b>IIBB</b><span>${pct(t.iibb)}</span><b>Tasa</b><span>${pct(t.statisticalFee)}</span></div><div>Honorarios: <b>${t.honorariaApplies?'Aplica':'No aplica'}</b></div>`;$('#logPreview').innerHTML=(l.lines||[]).map(x=>`<div class="summary-row"><span>${x.name} <small>(${x.basis})</small></span><b>${money(x.amount)}</b></div>`).join('');calculateCurrent(t,l)}
async function calculateCurrent(t,l){state.calc=await api('/api/calculate',{method:'POST',body:{...state.draft,taxProfile:t,logisticsProfile:l}});const c=state.calc;$('#quoteSummary').innerHTML=`<div class="summary-list"><div class="summary-row"><span>FOB</span><b>${money(c.fob)}</b></div><div class="summary-row"><span>Base CIF</span><b>${money(c.cif)}</b></div><div class="summary-row"><span>Derecho</span><b>${money(c.duty)}</b></div><div class="summary-row"><span>IVA</span><b>${money(c.vat)}</b></div><div class="summary-row"><span>IVA adicional</span><b>${money(c.vatAdditional)}</b></div><div class="summary-row"><span>Ganancia</span><b>${money(c.earnings)}</b></div><div class="summary-row"><span>IIBB</span><b>${money(c.iibb)}</b></div><div class="summary-row"><span>Tasa</span><b>${money(c.statisticalFee)}</b></div><div class="summary-row"><span>Honorarios</span><b>${money(c.honoraria)}</b></div><div class="summary-row"><span>Gastos logísticos</span><b>${money(c.logisticsTotal)}</b></div><div class="summary-row"><span>Recupero impuestos</span><b>-${money(c.recoverable)}</b></div><div class="summary-total"><span>Total a abonar</span><span>${money(c.totalToPay)}</span></div><div class="summary-row"><span>Costo total con mercadería</span><b>${money(c.landedCost)}</b></div><div class="summary-row"><span>Costo neto luego recupero</span><b>${money(c.netCost)}</b></div></div>`}
async function saveQuote(emit){readQuote();const r=await api('/api/quotes',{method:'POST',body:{...state.draft,status:emit?'issued':'draft'}});alert(`Guardada ${r.quoteNo}`);await refresh();route(state.draft.mode==='logistics'?'logistics':'quotes')}
function catalog(){shell('Generador de catálogos','Seleccioná productos y generá una presentación comercial.');const ps=state.data.products;$('#pageBody').innerHTML=`<div class="card"><div class="product-list">${ps.map(p=>`<div class="product-card"><img src="${p.imageUrl||'/assets/shenzhen-logo.png'}"><h4>${p.name}</h4><small>${p.sku||p.id} · ${money(p.fob)} · ${Number(p.cbm||0).toFixed(3)} CBM</small></div>`).join('')||'<p>Cargá productos para generar catálogo.</p>'}</div></div>`}
function clients(){shell('Clientes','Base de clientes para cotizaciones.',`<button class="btn primary" onclick="clientModal()">+ Nuevo cliente</button>`);$('#pageBody').innerHTML=`<div class="card"><table><thead><tr><th>Nombre</th><th>Email</th><th>CUIT</th></tr></thead><tbody>${state.data.clients.map(c=>`<tr><td>${c.name||'-'}</td><td>${c.email||'-'}</td><td>${c.taxId||'-'}</td></tr>`).join('')||'<tr><td colspan="3">Sin clientes.</td></tr>'}</tbody></table></div>`}
function clientModal(){openModal('Nuevo cliente',`<div class="form-grid"><div class="field span2"><label>Nombre</label><input id="cName"></div><div class="field"><label>Email</label><input id="cEmail"></div><div class="field"><label>CUIT</label><input id="cTax"></div></div><br><button class="btn primary" onclick="saveClient()">Guardar</button>`)}async function saveClient(){await api('/api/clients',{method:'POST',body:{name:$('#cName').value,email:$('#cEmail').value,taxId:$('#cTax').value}});closeModal();await refresh();route('clients')}
function openModal(title,html){$('#modalTitle').textContent=title;$('#modalBody').innerHTML=html;$('#modal').classList.remove('hidden')}function closeModal(){$('#modal').classList.add('hidden')}
async function refresh(){state.data=await api('/api/bootstrap');applyTenant();renderNav()}
async function setTenant(t){state.tenantId=t;localStorage.setItem('tenantId',t);await refresh();route('dashboard')}
function switchTenant(){$('#tenantSelect').focus()}
boot();
